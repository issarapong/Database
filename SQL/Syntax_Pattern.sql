-- Query Patern

SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
LIMIT




-- ประมวลผลจาก From เพื่อหา TAble จากนั้นไป  สั่ง WHERER และแล้วไป Group by แล้วค่อย ไป Select เสร็จแล้ว ถึง Having แล้ว ORDER แล้วถึง Limit

FROM
WHERE 
GROUP BY 
HAVING 
ORDER 
LIMIT